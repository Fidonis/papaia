version: 1.0.0
mcpServers:
  FirecrawlMCP:
    type: streamable-http
    url: http://mcp-firecrawl:3000/mcp
mcpSettings:
  allowedDomains:
    - "http://mcp-firecrawl:8080"
webSearch:
  serpProvider: searxng
